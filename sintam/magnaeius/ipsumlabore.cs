foreach (var fk in Fk)
{
    // Code to be executed for each element in Fk
    // The variable fk represents the current element in the loop
    // You can perform operations or access properties of fk within the loop
}
